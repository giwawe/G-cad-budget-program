{
  "name": "cad-budget-program-g",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "dev:api": "python -m uvicorn server.app.main:app --reload --host 127.0.0.1 --port 8000",
    "test:api": "python -m pytest server/tests -v"
  },
  "workspaces": [
    "apps/web"
  ]
}

